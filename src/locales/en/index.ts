import { home } from './home'
import { alt } from './alt'
import { topbar } from './../en/topbar'
import { course } from './course'

const en = {
  topbar,
  alt,
  home,
  course,
}
export default en
